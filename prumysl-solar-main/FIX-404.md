# 🔧 إصلاح خطأ 404 على GitHub Pages

## المشكلة
GitHub Pages يعرض خطأ 404 عند فتح الموقع.

## الحل السريع | Quick Fix

### الخطوة 1: تأكد من الملفات المطلوبة ✅

تأكد من وجود هذه الملفات في **جذر المستودع** (root):

```
✅ index.html          (الصفحة الرئيسية)
✅ 404.html            (صفحة إعادة التوجيه)
✅ .nojekyll           (تعطيل Jekyll)
✅ CNAME               (إذا كان لديك نطاق مخصص)
✅ css/style.css       (ملف التنسيقات)
✅ img/                (مجلد الصور)
```

### الخطوة 2: إعدادات GitHub Pages ⚙️

1. اذهب إلى **GitHub Repository**
2. اضغط على **Settings** (الإعدادات)
3. اضغط على **Pages** من القائمة الجانبية
4. في قسم **Source**:
   - اختر **Deploy from a branch**
   - **Branch**: اختر `main` (أو `master` إذا كان اسم الفرع `master`)
   - **Folder**: اختر `/ (root)`
5. اضغط **Save**

### الخطوة 3: انتظر النشر ⏱️

- انتظر **1-2 دقيقة** بعد الحفظ
- GitHub Pages يحتاج وقت للنشر

### الخطوة 4: تحقق من النشر ✅

افتح الموقع:
- `https://[username].github.io/[repository-name]/`
- أو `https://prumysl.cc` إذا كان CNAME مضبوط

## إذا استمرت المشكلة

### الحل 1: تحقق من اسم المستودع

إذا كان اسم المستودع `prumysl-solar-main`، الرابط سيكون:
```
https://[username].github.io/prumysl-solar-main/
```

### الحل 2: تحقق من الفرع

تأكد أنك ترفع الملفات على الفرع الصحيح (`main` أو `master`).

### الحل 3: استخدم روابط `.html` مباشرة

في HTML، استخدم:
```html
<a href="/index.html">الرئيسية</a>
<a href="/moka.html">موكا</a>
```

بدلاً من:
```html
<a href="/">الرئيسية</a>
<a href="/moka">موكا</a>
```

### الحل 4: تحقق من الملفات المرفوعة

تأكد أن جميع الملفات موجودة في GitHub:
- `index.html`
- `404.html`
- `.nojekyll`
- `css/style.css`
- جميع ملفات HTML الأخرى

## ملاحظات مهمة ⚠️

1. **`.htaccess` لا يعمل على GitHub Pages** - هذا خاص بـ Apache فقط
2. **`_redirects` لا يعمل على GitHub Pages** - هذا خاص بـ Netlify فقط
3. **`.nojekyll` ضروري** - لتعطيل معالجة Jekyll
4. **`404.html` يعيد التوجيه** - للروابط بدون `.html`

## بدائل GitHub Pages

إذا استمرت المشاكل، يمكنك استخدام:

1. **Netlify** - يدعم `_redirects` ✅
2. **Vercel** - يدعم `vercel.json` ✅
3. **Apache Server** - يدعم `.htaccess` ✅
